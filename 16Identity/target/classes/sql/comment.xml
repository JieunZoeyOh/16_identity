<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="Comments">
	<insert id="insert" parameterType="C">
		INSERT INTO comments (cmt_no, isbn, cmt_mbti, cmt_id, cmt_content,  cmt_date) values(cmt_no.nextval, #{isbn}, #{cmt_mbti}, #{cmt_id}, #{cmt_content},  sysdate)
	</insert>
	
	<select id="member_mbti" parameterType="String" resultType="String">
		select m_mbti from member where m_id=#{id}
	</select>
	
	
</mapper>