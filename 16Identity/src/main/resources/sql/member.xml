<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="Members">
	<select id="statelist" resultType="member">
		select m_id, substate,m_mbti from member
	</select>
	<select id="idcheck" parameterType="String" resultType="member">
		select *
		from member where m_id = #{m_id}
	</select>
	
	<insert id="insert" parameterType="member">
		insert into member (m_id, m_name, m_nickname, m_original, m_file, m_password, m_mbti, m_phone, m_address_no, m_address)
		values (#{m_id}, #{m_name}, #{m_nickname}, #{m_original}, #{m_file}, #{m_password}, #{m_mbti}, #{m_phone}, #{m_address_no}, #{m_address})
	</insert>
</mapper>