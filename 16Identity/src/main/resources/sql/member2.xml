<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="Members2">

<insert id="insert" parameterType="member2">
		insert into testmember values 
			((select nvl(max(num), 0)+1 from testmember), #{name}, #{email}, #{tel}, #{addr}, sysdate)
	</insert>
	
	<select id="read" resultType="member2" parameterType="int">
		select * from testmember where num = #{num}
	</select>
	
	<select id="readAll" resultType="member2">
		select * from testmember
	</select>
	
	<insert id="registerDept" parameterType="java.util.Map">
		insert into dept values (#{deptno}, #{dname}, #{loc})
	</insert>
	
	<insert id="register" parameterType="java.util.Map">
		insert into members values (#{userid},#{passwd},#{name},#{email},#{tel})
	</insert>
</mapper>